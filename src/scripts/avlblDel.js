import Goods from '../modules/CMR/models/GoodsModel';
import Params from '../modules/CMR/models/ParamsModel';

export const avlblDel = async () => {
  // const goods = await Goods.deleteMany({avlbl: "false"}).exec()
  // const goods = await Goods.deleteMany({img: []}).exec()
  // const goods = await Params.collection.drop();
  // console.log(goods);
}